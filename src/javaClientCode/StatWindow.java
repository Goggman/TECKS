package javaClientCode;
import java.io.IOException;
import java.io.PrintWriter;
import java.util.ArrayList;



import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.TextField;
import javafx.scene.layout.Pane;

import javafx.scene.paint.Paint;
import javafx.stage.Stage;
import javafx.scene.control.MenuButton;
import javafx.scene.control.MenuItem;
public class StatWindow implements Window { //TODO: Make a statistics window, or integrate elements of this window into others
	StatWindow(Stage stageIn, GUIController ctrlIn, ServerClient clientIn){
	}
	public Scene createScene(){
		
	return new Scene(new Pane());
	}
	
	public void wakeUp(){
		
	}
	public void sleep(){
		
	}
}
